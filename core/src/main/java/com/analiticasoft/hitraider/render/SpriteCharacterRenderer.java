package com.analiticasoft.hitraider.render;

import com.badlogic.gdx.graphics.glutils.ShapeRenderer;

/**
 * Placeholder: cuando tengamos sprites, esta clase cambiará a usar SpriteBatch.
 * Por ahora no se usa.
 */
public class SpriteCharacterRenderer implements CharacterRenderer {
    @Override
    public void draw(ShapeRenderer shapes, float pivotX, float pivotY, int facingDir, FrameDef frame) {
        // TODO: reemplazar por render con sprites (SpriteBatch) cuando existan assets
    }
}
